# Bitget credentials are not needed for public candle data in early stages.
# Keep these empty until private endpoints are explicitly needed later.
API_KEY=
SECRET_KEY=
PASSPHRASE=

APP_NAME=crypto-strategy-optimizer-backend
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
